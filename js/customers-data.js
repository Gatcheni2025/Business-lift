import {onAuthStateChanged} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js";import {auth} from "./firebase-config.js";import {getWorkspaceOrders,workspaceError} from "./business-context.js";
const body=document.querySelector("[data-customer-rows]"),money=new Intl.NumberFormat("en-ZA",{style:"currency",currency:"ZAR"});
onAuthStateChanged(auth,async user=>{if(!user)return;try{const data=await getWorkspaceOrders(user),customers=new Map();(data.orders||[]).forEach(o=>{const id=o.customerName||o.customerId||o.customerEmail;if(!id)return;const x=customers.get(id)||{id,count:0,total:0};x.count++;x.total+=Number(o.total||0);customers.set(id,x)});body.innerHTML=customers.size?[...customers.values()].map(x=>'<tr><td>'+x.id+'</td><td>'+x.count+'</td><td>'+money.format(x.total)+'</td></tr>').join(""):'<tr><td colspan="3"><div class="empty-state"><h3>No customers yet</h3><p>Customers appear automatically from your Teyza orders.</p><a class="button" href="orders.html">Create an order</a></div></td></tr>'}catch(e){const s=document.querySelector("[data-customer-status]");if(s){s.hidden=false;s.textContent=workspaceError(e,"load customers")}body.innerHTML='<tr><td colspan="3">Customer activity is unavailable.</td></tr>'}});

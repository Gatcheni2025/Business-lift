import {onAuthStateChanged} from "https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js";import {auth} from "./firebase-config.js";import {getWorkspaceSection,saveWorkspaceSection,workspaceError} from "./business-context.js";let user;const ids=["category","serviceArea","offers","needs"];
function fill(d){ids.forEach(id=>document.getElementById(id).value=d[id]||"");document.getElementById("discoverable").checked=d.discoverable!==false;document.getElementById("allowContact").checked=d.allowContact!==false}
onAuthStateChanged(auth,async u=>{if(!u)return;user=u;try{fill((await getWorkspaceSection(u,"partner")).data||{})}catch(e){document.getElementById("networkStatus").textContent=workspaceError(e,"load network profile")}});
document.getElementById("saveNetwork")?.addEventListener("click",async e=>{const b=e.currentTarget;b.disabled=true;try{const d=Object.fromEntries(ids.map(id=>[id,document.getElementById(id).value]));d.discoverable=document.getElementById("discoverable").checked;d.allowContact=document.getElementById("allowContact").checked;await saveWorkspaceSection(user,"partner",d);document.getElementById("networkStatus").textContent="Saved ✓"}catch(err){document.getElementById("networkStatus").textContent=workspaceError(err,"save network profile")}finally{b.disabled=false}});
